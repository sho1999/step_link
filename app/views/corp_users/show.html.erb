<div class="padding-block"></div>

<div class="contents row">
  <% if corp_user_signed_in? && current_corp_user.id == @corp_user.id %>
    <%= link_to 'ログアウト', destroy_corp_user_session_path, method: :delete %>
    <%= link_to '編集', edit_corp_user_registration_path, method: :get %>
    <%= link_to 'アカウント削除', corp_user_registration_path, method: :delete %>
  <% end %>
  <h3>このページは仮のマイページです</h3>
  <p class="student_content">担当者：<%= @corp_user.nickname %></p>
  <p class="student_content">会社名：<%= @corp_user.corp_name %></p>
  <p class="student_content">所在地：<%= @corp_user.location %></p>
  <p class="student_content">業界名：<%= @corp_user.industry.name %>業界</p>
  <p class="student_content">特徴：<%= @corp_user.feature.name %></p>
  <p class="student_content">会社から一言：<%= @corp_user.staff_msg %></p>
</div>